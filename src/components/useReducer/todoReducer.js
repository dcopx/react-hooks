
export const todoReducer = (initialState = [], action = {}) => {

    switch (action.type) {
        case 'abc':

            break;
        default:
            initialState
            break;
    }

}